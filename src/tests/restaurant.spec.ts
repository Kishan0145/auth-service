// Restaurant tests are pending implementation.
// The route requires auth + role-based middleware, so tests need a pre-authenticated
// admin/manager token — that setup will be added alongside the full test suite.
describe('Restaurant Testing', () => {
   it.todo('POST /api/restaurant/register should return 201');
});
